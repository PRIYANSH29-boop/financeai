"""Test package marker for `audit`. Present so pytest resolves these modules as a package
rather than by rootdir inference, which is what keeps same-named test modules in different
packages from shadowing each other."""
