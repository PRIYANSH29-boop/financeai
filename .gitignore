# Python
venv/
__pycache__/
*.pyc
*.pyo
.Python
.pytest_cache/

# Environment variables (API keys etc) — NEVER commit secrets
.env
.streamlit/secrets.toml

# Data files
data/*.csv
data/*.json
data/*.db
data/*.parquet
data/*.png
data/cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/
output.txt

# Analytics — live user-generated charts (the committed ones have no _live_ prefix)
figures/analytics/_live_*

# FMP fundamentals raw JSON cache (#17 audit) — regenerable
data/fundamentals_cache/

# SEC EDGAR XBRL extraction cache (#17/#18) — regenerable from data.sec.gov, ~13 MB
data/sec_cache/

# Scratch space for run logs / throwaway scripts
scratchpad/
