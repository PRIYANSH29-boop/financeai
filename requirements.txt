# RankAlpha — runtime dependencies (Python 3.10+)
# Pinned to versions verified working; loosen as needed.

# Core data / numerics
pandas>=2.2
numpy>=1.26
pyarrow>=15            # parquet engine for the data + ledger files

# Model + explainability
lightgbm>=4.3          # LGBMRanker (LambdaMART) — the frozen cross-sectional ranker
shap>=0.45             # per-holding SHAP factor attributions
scikit-learn>=1.4      # transitive support for shap / model utilities

# Product + figures
streamlit>=1.40        # demo product page (app.py)
matplotlib>=3.8        # pie / factor / risk / track-record figures
joblib>=1.3            # on-disk cache of the fitted book + frozen model

# Data fetching (S&P 500 panel build) + LLM explainer HTTP (Ollama / Groq REST)
requests>=2.31
beautifulsoup4>=4.12   # scrape current S&P 500 constituents

# Optional: local LLM explainer (set USE_LLM=1). Requires a separate Ollama install
# (`ollama serve` + `ollama pull phi3:mini`); no Python package needed — called over HTTP.
